language: python
notifications:
  email: false
env:
  global:
python:
  - 2.6
  - 2.7
install:
  - python setup.py install
  - pip install -r tests/requirements.txt --use-mirrors
script:
  nosetests --rednose --with-cov
after_success:
  codecov
